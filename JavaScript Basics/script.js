/*
alert ("Hello World!");

      let app = 'Creating Modren JavaScript ES6+ Applications';
      if (app === 'Creating Modren JavaScript ES6+ Applications') 
            alert ('You have assigned with Modern JavaScript Project');
      console.log(11 + 21 + 31);
  */

// Values and Variables - Value can be primitive or object to a variable
/*
let firstName = "Rick Jhon";
console.log(firstName);  // Use camelCase Rule

// let 5years = 5; // Illegal 
// let year&moth = "2021-01";
// let new = "JS";
let $function = "JavaScript Function";

const PI_VALUE = 3.14;

// Names of Variables should be more descriptive
let courseInterested = "React";
let courseRegistered = "MERN Full Stack Web Development";
console.log(courseRegistered);
*/


// Data Types - 7 Primitive Data Types
// Note: JavaScript supports dynamic typing - data types are determined automatically
// Value has type, NOT variable..
/*
let  age = 23 // Number - used for decimals & integers
let firstName = 'Rick'; // String
let  courseRegistered = true; // Boolean
console.log(courseRegistered);
console.log( typeof age );  // Using typeof operator

firstName = 'Jhon';  // Assigning a new value to a variable

// Undefined - value taken by a variable that is not defined (empty value)
let salary;
console.log(salary);
console.log(typeof salary);  

//Null - Empty Value

// Symbol(ES2015) - value that is unique and can't be changed.
// BigInt(ES2020) - Larger integers than the 'Number' type can hold
*/
 

//  let, const ( introduced ES6) and var (old way of declaring varibles)
let age = 25;
age = 26 // Mutated (Reassigning later) variable

const BIRTH_YEAR = 1989;
 // BIRTH_YEAR =1990; // Error (Immutable variable, a variable that can't be mutated)

 /* Note: Use 'const' by default and use 'let' only when you are sure that the variable needs to
 change at some point future.
 */


